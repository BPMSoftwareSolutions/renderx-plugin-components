# NPM configuration for @renderx-plugins/components

# Ensure public access for scoped packages
access=public

# Use npm registry
registry=https://registry.npmjs.org/

# Save exact versions in package-lock.json
save-exact=true

# Automatically save dependencies
save=true

# Use package-lock.json for reproducible builds
package-lock=true
